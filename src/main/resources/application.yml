spring:
  datasource:
    url: jdbc:mysql://localhost:3306/store
    username: store
    password: store
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
      show_sql: true